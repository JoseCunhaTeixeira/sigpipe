[project]
name = "sigproc"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy",
    "scipy",
    "h5py",
    "matplotlib",
    "tqdm",
    "dash",
    "pyyaml",
    "pandas",
    "torch",
]

[tool.ruff]
target-version = "py314"