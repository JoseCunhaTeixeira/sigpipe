[project]
name = "sigpipe"
version = "1.0.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy",
    "scipy",
    "h5py",
    "matplotlib",
    "tqdm",
    "pyyaml",
    "pandas",
    "obspy",
    "disba",
    "bayesbay",
]

[project.optional-dependencies]
# Only needed for Beamform(method="cross") / BEAMFORMING_METHODS["cross"];
# not required to import or use the rest of sigpipe.
beamforming = ["torch"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
sigpipe = ["py.typed"]

[tool.mypy]
python_version = "3.14"
strict = true

[[tool.mypy.overrides]]
module = [
    "bayesbay.*",
    "disba.*",
    "scipy.*",
    "h5py.*",
    "torch.*",
    "obspy.*",
    "pandas.*",
    "matplotlib.*",
    "pyyaml.*",
]
ignore_missing_imports = true
